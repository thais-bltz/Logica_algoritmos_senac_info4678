<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <input type="text" id="dias">
    <button onclick="ExibeDadoDigitado()">Click Aqui</button>
<script>
  function ExibeDadoDigitado(){
    var dias = document.getElementById("dias").value;
    var HorasTrabalhadas = 8;
    var pagamento = 25;
    var resultado = Number(HorasTrabalhadas) * Number(pagamento);
    var resultado2 = resultado * Number(dias);

            alert("  " + " = " + resultado2)
        }
</script>
</body>
</html>